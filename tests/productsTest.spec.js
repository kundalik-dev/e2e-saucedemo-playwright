import { test, expect } from "@playwright/test";

test.describe("handling Products ", () => {
  test("add product to cart", async ({ page }) => {});
});

/**
 * Test cases
 * 1. Add products to carts
 * 2. Verify products present on page
 * 3. check product price after sorting lohi
 * 4. Sort product and check product price
 * 5. Count number of products present
 */
